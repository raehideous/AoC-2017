const fs = require('fs');

let input = fs.readFileSync('test.txt', 'utf8').split('\r\n');

let initialPattern = ['.#.','..#','###'];


function processPattern(pattern) {
	let size = pattern.length;
	
	//console.log(sPattern);
	if(size % 2 === 0) {
		
		
	} else if (size % 3 === 0) {
		
		
	}
}

processPattern(initialPattern);